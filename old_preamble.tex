%\usepackage[normalem]{ulem} % gives a better possibility to underline text (e.g. across line breaks)

%\usepackage{multicol} % gives an environment to typeset text in multiple columns

%\usepackage{perpage} % provides a mechanism to reset counters per page and/or keep their occurences sorted in order of appearance on the page

%\setlength{\itemsep}{0.1ex} % set distance between items

%\setcounter{secnumdepth}{3} % number subsubsections

%\setcounter{tocdepth}{2} % include up to \subsections in table of contents

%\RedeclareSectionCommands[tocdynnumwidth]{chapter,section,subsection} % dont know if that does anything?

%\usepackage{subcaption} % package to include subfigures